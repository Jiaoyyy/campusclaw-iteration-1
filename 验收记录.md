# CampusClaw 迭代 1 验收记录

日期：2026-09-23。环境：macOS arm64、Docker Desktop 29.8.0、Node.js 24，Docker Compose 单实例，访问地址 `http://127.0.0.1:8080/`。此记录只写已实际执行的检查。

2026-09-24 在仍运行的 Docker 服务上重新执行同一组测试：2 个通过、0 个失败。

## 结构校验

执行：`openspec validate add-auth-rbac-class-knowledge --strict`

结果：退出码 0，输出 `Change 'add-auth-rbac-class-knowledge' is valid`。

## 自动运行检查

执行：`TEST_BASE_URL=http://127.0.0.1:8080 npm test`

结果：2 个测试通过、0 个失败。测试源码在 `test/flow.test.js` 和 `test/failure.test.js`。故障注入测试使用系统临时目录中的独立数据库，不修改 Docker 中的课程资料。

| 场景 | 实际判定与证据 |
| --- | --- |
| 未登录访问材料 API | 401，无资料数据；`flow.test.js` 断言通过 |
| 正确与错误密码 | 正确密码 200；错误密码 401；`flow.test.js` 断言通过 |
| 教师上传中文 Markdown | 201；班级为 A，材料和知识库表均有对应记录；`flow.test.js` 断言通过；浏览器出现“上传成功”及中文标题 |
| A 班学生查看本班资料 | 列表、详情和下载可用；`flow.test.js` 断言通过 |
| B 班查看 A 班材料 | 列表和标题搜索不含该条；直接访问详情和下载均 404，响应不含讲义正文；`flow.test.js` 断言通过 |
| 学生调用上传 API | 403，材料记录数不增加；`flow.test.js` 断言通过 |
| 缺少 CSRF 令牌上传 | 403；`flow.test.js` 断言通过 |
| 退出后重用旧 Cookie | 401；`flow.test.js` 断言通过 |
| 会话过期后重用 Cookie | 401；隔离测试库中强制设置过期时间，`failure.test.js` 断言通过 |
| 空文件、无效 UTF-8、超限文件 | 分别 400、400、413；`flow.test.js` 与 `failure.test.js` 断言通过 |
| 数据库写入中途失败 | 503；材料表、知识库表均无残留行，活动上传目录无残留文件；`failure.test.js` 断言通过 |
| 进程中断遗留孤立文件 | 重启后移入 `.orphaned/`，不作为可见资料；`failure.test.js` 断言通过 |

## 浏览器与 Docker 实测

- 浏览器以 `teacher-a` 登录，能看到教师上传区；上传 `课堂验收讲义.md` 后显示“上传成功”，列表出现中文标题，详情渲染出一级标题和正文。
- 在浏览器里搜索“课堂验收”只显示相应讲义；浅色/深色和列表/网格切换的页面状态实际变化。
- 上传包含 `<img onerror=...>` 与 `javascript:` 文本的 Markdown 后，详情中它们只作为文字显示；页面内生成的图片与链接节点数均为 0。
- Docker `compose up --build -d` 成功，`compose ps` 显示服务健康，`GET /health` 返回 `{"status":"ok"}`。
- 重启容器后，已上传的讲义仍出现在列表中。
- 曾发现中文文件名乱码和上传后页面提示错误，均已修复并在 Docker 版浏览器中重新验证。既有乱码标题已在备份数据库后修正，原文件和正文保留。

## 逐条对照规约 Scenario

| 规约场景 | 当前结论 | 证据或待办 |
| --- | --- | --- |
| 正确与错误口令、未登录访问、登出后重放 | 已通过 | `test/flow.test.js`；页面以教师账号登录过 |
| 学生直接上传 | 已通过 | `test/flow.test.js` 返回 403；服务端在写入前检查角色 |
| 本班列表与搜索、跨班 ID 和下载、上传伪造归属 | 已通过 | `test/flow.test.js` 断言 A/B 班隔离、404 和伪造字段无效 |
| 教师上传成功、非法文件拒绝、上传中途失败 | 已通过 | 两个测试文件及上文的数据库与文件检查 |
| 同班学生阅读、切换显示方式 | 已通过 | API 测试及上文浏览器实测 |
| 第三方启动与重启 | 本机已通过，真实第三方待验 | 本机 Compose、健康检查和重启持久化已测；同伴尚未复现 |
| 密钥与口令检查 | 已通过 | `.env.example` 在仓库，真实 `.env` 不在提交历史；`server.js` 用 scrypt 加盐哈希 |
| 数据库故障 | 部分通过 | 已注入知识库表写入失败并得到 503；未单独模拟数据库文件整体不可用时 `/health` 的行为 |
| 完成交付审查 | 未完成 | 真实同伴互验、本人复核设计、OpenSpec 归档和版本 tag 待完成 |

规约中的“逐条验收”按此表核对；“部分通过”和“未完成”不能算全项通过。

## 提交前仍需本人完成

- 本人阅读规约和设计并能解释：为什么学生上传是 403、跨班资源是 404、文件与数据库写入失败如何处理。
- 找同伴按 README 在其环境复现，并实际尝试跨班 URL；记录同伴姓名或标识、时间、操作与结果。**这一步尚未发生，不能勾选或伪造。**
- 个人公开 GitHub 仓库已建立；版本标记、OpenSpec 归档和课程作业提交仍待完成。作业页面截图显示截止时间为 2026-09-30 00:00，提交要求 `.txt.markdown` 附件；具体页面网址尚未提供。
