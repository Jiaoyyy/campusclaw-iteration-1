# 当前进度（2026-09-24）

## 已实际运行

- 本机用 Node.js 24 启动网站，浏览器打开登录页，并以 A 班教师账号登录，看到资料列表、详情和下载入口。
- `npm test` 通过。测试覆盖未登录拒绝、正确/错误密码、教师上传双表入库、伪造班级字段、同班可读、跨班列表和详情/下载隔离、学生上传 403、缺 CSRF 拒绝、空文件拒绝、退出后旧 Cookie 失效。
- OpenSpec 官方 CLI 执行 `openspec validate add-auth-rbac-class-knowledge --strict`，输出 `Change 'add-auth-rbac-class-knowledge' is valid`。
- Docker Compose 已成功构建并启动官方 Node 24 镜像；`http://127.0.0.1:8080/health` 返回 `{"status":"ok"}`。浏览器在 Docker 版完成教师登录；对 Docker 版执行同一组 `npm test`，结果 1/1 通过。重启容器并刷新页面后，测试上传的讲义仍在列表中。
- 后续补充故障注入与中文上传检查：Docker 版 `npm test` 2/2 通过；浏览器里中文文件名、上传成功提示、搜索、详情、主题与视图切换均已验证。完整记录见《验收记录.md》。
- 完整源码已推送至个人私有仓库：[Jiaoyyy/campusclaw-iteration-1](https://github.com/Jiaoyyy/campusclaw-iteration-1)。仓库不包含本机密码文件、数据库或上传资料。

## 尚未完成

- 仍需由本人阅读规约；还需真实同伴互验。磁盘故障的独立注入尚未测试，数据库失败和孤立文件恢复已有测试。
- 本人阅读和解释设计、真实同伴互验、OpenSpec Archive、版本 tag、作业平台提交均尚未完成。
- 仓库目前为私有；老师或同伴要查看，需要获得访问权限，或者由本人决定是否改为公开。

## 给初学者的下一步

在本机启动后打开 `http://127.0.0.1:8080/`，先用 `teacher-a` 登录，再用 `student-a` 比较学生页面是否没有上传入口。测试密码保存在同目录的隐藏文件 `.env` 中；请勿把它放进作业仓库。然后找一位真实同伴按 README 复现，并记录姓名或标识、时间与结果。自动测试已验证学生直接调用上传接口也会被拒绝。
